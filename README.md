# Boss招聘小助手

> Boss直聘求职效率工具：抓取岗位 JD → 上传简历（PDF / Word / TXT）→ AI 定制优化 → 选岗匹配 → 生成专属话术 → 一键投递。
>
> 当前版本：**v1.4.0**

---

## ✨ 功能

| 能力 | 说明 |
|---|---|
| 🎯 抓取 JD | 在职位详情页一键捕获「岗位职责 / 任职要求」，多层选择器容错，抓不到可手动粘贴 |
| 📄 上传简历 | 支持 **PDF / Word(.docx) / TXT / MD**，也可直接粘贴全文；数据仅存本地浏览器 |
| ✨ AI 优化 | 按 JD 定制改写简历：关键词前置、STAR 量化、严禁虚构；优化后**自动生成该岗位打招呼话术** |
| 🔍 选岗匹配 | 在职位列表页扫描本页岗位，按简历匹配度打分（AI 或本地关键词），绿标=合适，可只看合适岗位 |
| 🎨 简历版式 | 内置 **现代简约 / 深色商务 / 雅致衬线** 三套模板，导出的 PDF 与图片都套用该版式 |
| ⬇️ 导出 | 一键导出 **PDF**（多页 A4、矢量中文可选中可搜索、**不依赖网络**）或 **.doc**（可再编辑），也可存高清长图 |
| 🖼 一键投递 | 在 HR 聊天页把简历渲染成**图片**直接发送（手机端预览清晰），或填入 AI 话术 |
| 🗣 话术模板 | 内置正式/精简/活泼三套，可自定义多套，支持 `{岗位}{公司}{薪资}{姓名}` 占位符 |
| 🔄 自动更新 | 脚本支持油猴自动检查更新（需部署到你自己的仓库） |

---

## 📦 安装

1. **装浏览器扩展**：Chrome / Edge 安装 [Tampermonkey（篡改猴）](https://www.tampermonkey.net/)，Firefox 可装 Greasemonkey。
2. **装脚本**：点下方链接，篡改猴会自动弹出安装页 → 点「安装」。
   ```
   https://gitee.com/zzc356/boss-recruit-helper/raw/master/boss-recruit-helper.user.js
   ```
   > 若没弹出安装页，复制链接 → 篡改猴面板 → 实用工具 → 导入 → 从 URL。
3. **打开 Boss 直聘**：`https://www.zhipin.com`，页面右下角出现 💼 悬浮按钮即安装成功。

### ⚠️ Edge 用户必看（高频坑）

Edge 138+ 之后，**光开「开发人员模式」不够**，还要单独给篡改猴开权限：

1. 地址栏输入 `edge://extensions/`
2. 打开左下角 **「开发人员模式」**
3. 找到「篡改猴」→ 点 **「详细信息」**
4. 往下翻，打开 **「允许用户脚本」（Allow User Scripts）** ← 关键
5. 同一页把 **「网站访问」设为「在所有网站上」**
6. **完全关闭 Edge 再重开**（不是刷新标签页），然后打开 Boss 页面按 `Ctrl+F5`

> 判断是否生效：篡改猴图标出现**数字角标**、下拉菜单里能看到「打开/收起小助手面板」。

---

## ✨ 功能一览

| 模块 | 功能 |
|---|---|
| 📋 JD | 一键抓取当前岗位 JD（职位详情页 / 聊天页岗位卡），可手改补充 |
| 📄 简历 | 上传/粘贴简历（PDF / Word / TXT / MD 自动解析，全程本地、不上传任何服务器） |
| 🔍 选岗 | 按简历匹配度为职位列表打分、筛选，离线也能用 |
| ✨ AI 优化 | 按目标岗位 JD 优化简历（STAR 量化改写），一键完成 |
| 🎯 HR 智能回复 | 聊天页自动抓取 HR 最新提问，结合优化后的简历与 JD 生成第一人称针对性回复 |
| ✍️ AI 话术 | 自动生成多风格打招呼话术，支持自建话术模板 |
| 📄 导出 | 直接下载 PDF / 打印矢量 PDF / 长图 / .doc，6 套简历版式可选 |
| 🖼 发送 | 优化简历渲染成图片，一键附加到 HR 对话 |
| 🔑 授权 | 支持作者授权激活，未激活时付费功能弹出授权框（面板 → 🔑 激活授权） |

### 在线激活网页

- 买家访问：`https://zzc356.gitee.io/boss-recruit-helper/activate.html`（脚本里「获取授权」按钮已指向它）
- 网页功能：① 验证授权有效性；② 展示本机设备标识供发给作者；③ 购买方式展示（`activate.html` 顶部 `CONFIG.buy` 配置微信 / QQ / 闲鱼等）；④ 安装指引

---

## ⚙️ 配置 AI

面板 → **⚙️ 设置** → 填「接口地址」+「API Key」+「模型名称」→ 保存 → 点「测试连通」。

| 服务 | Base URL | 模型示例 |
|---|---|---|
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` |
| Kimi | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |
| OpenAI | `https://api.openai.com/v1` | `gpt-4o-mini` |

> 没配 Key 也能用「🔍 选岗」的**本地关键词匹配**，但 AI 优化与话术生成需要 Key。

---

## 🚀 使用流程

1. **传简历**：「📄 简历」页上传 PDF/Word 或粘贴全文（一次即可，跨页面记住）
2. **选岗位**：「🔍 选岗」页，在职位列表页点扫描 → 挑合适的岗位点「应用此岗位」
3. **抓 JD**：「📋 JD」页点抓取（应用岗位后通常已自动带上）
4. **优化**：「✨ 优化」页点「🚀 开始优化」→ 等待 10~60 秒
5. **导出 / 投递**：点「📄 导出 PDF 文件」**直接下载 .pdf 文件**（多页 A4 自动排版）；要文字可复制的矢量 PDF 用「🖨 打印 / 另存为 PDF」；或在 HR 聊天页点「🖼 向当前对话发送简历图片」直接发送。共六套版式可在「🎨 切换版式」里挑选

---

## 🔄 自动更新（部署者看）

本项目已内置更新检查，脚本托管在 **Gitee** `https://gitee.com/zzc356/boss-recruit-helper`，用户端会自动收到新版本。

脚本头部两行固定指向本仓库的 raw 文件（已填好，无需改动）：
```js
// @updateURL    https://gitee.com/zzc356/boss-recruit-helper/raw/master/boss-recruit-helper.user.js
// @downloadURL  https://gitee.com/zzc356/boss-recruit-helper/raw/master/boss-recruit-helper.user.js
```

**发版流程**（本仓库默认分支为 `master`）：

1. 改脚本里的 `@version` 与 `VERSION`（如 `1.6.1`）
2. 重新生成售卖版脚本与预览页（构建脚本会校验注入点，匹配不上会明确报错）
3. 把更新后的脚本、`activate.html`、`preview-resume.html` 复制进本仓库，双击 `update-and-push.bat` 提交推送
4. 用户端：篡改猴会自动检查并提示更新；也可在面板「⚙️ 设置」点「🔄 检查更新」

> 若 Gitee raw 被限流导致更新失败，用户可在「⚙️ 设置 → 云端更新地址」填其他镜像地址覆盖默认地址。

```bash
git add .
git commit -m "release v1.6.0"
git push origin master
```

---

## ❓ 常见问题

**Q：装了脚本但页面没反应？**
九成是 Edge 权限问题，见上文「Edge 用户必看」。另确认地址是 `www.zhipin.com`。

**Q：抓不到 JD？**
Boss 改版可能导致选择器失效，直接在「📋 JD」页手动粘贴 JD 正文即可，不影响后续流程。

**Q：导出 PDF 没反应？**
「📄 导出 PDF 文件」会直接下载 .pdf 文件（首次需联网加载导出组件）。若组件加载失败会自动打开打印窗口兜底：在打印框选「另存为 PDF」即可，记得勾「背景图形」；若打印窗口被拦截（地址栏右侧"已阻止弹出式窗口"），点它选「始终允许 zhipin.com 弹出式窗口」后重试。

**Q：PDF / 图片渲染依赖网络吗？**
PDF / 图片导出优先用内置的 html2canvas + jsPDF 组件生成真实文件；组件加载失败（断网等）时自动降级：图片走 SVG 原生渲染、PDF 走浏览器打印窗口，断网也能导出。只有**上传 PDF/Word 简历的解析**需要 CDN，受限时可改为复制粘贴正文。

**Q：发送简历图片时提示"已下载，请手动发送"？**
Boss 聊天页偶尔会拦截自动附加，脚本会自动把图片下载到本地，点聊天窗口的「图片」按钮手动上传即可。

**Q：数据会上传吗？**
不会。简历、JD、API Key 全部存在本机浏览器（篡改猴存储），仅 AI 优化请求你配置的模型接口。

---

## 📝 更新日志

**v1.6.0**
- 新增 **🎯 HR 智能回复**：「💬 聊天」页自动抓取 HR 最新提问（抓不到可手动粘贴），结合**优化后的简历**与岗位 JD 生成第一人称针对性回复，一键复制 / 填入聊天框，可「换个说法」重新生成

**v1.5.0**
- 授权机制升级：支持按设备授权、按功能档位解锁，激活后导出的 PDF / 图片带溯源水印；面板新增 **🔑 激活授权** 独立页签
- **PDF 导出重写**：「导出 PDF 文件」直接下载真实 .pdf（多页 A4 自动排版），不再依赖手动打印；打印窗口降级链路保留
- **修复长图 / PDF 内容截断**：修复高度测量取错元素导致的截断 bug
- **版式扩到六套**：新增 深海渐变（渐变横幅）/ 活力橙红（时间线）/ 青玉留白（极简双线）

**v1.4.0**
- **PDF 导出重写**：改用浏览器原生打印生成**矢量中文 PDF**（可选中 / 可搜索），彻底解决"下载后浏览器打不开"
- **图片渲染重写**：改用 SVG 原生管线，断网也能导出
- **新增「雅致衬线」版式**（共三套），标题改为三段式（色条 + 文字 + 渐变横线），项目符号改菱形，技能类分区自动渲染成标签云
- 页眉单独提取手机/邮箱，自动去掉正文里重复的联系方式与 `**` 标记
- 新增 `preview-resume.html`：三套版式并排预览、直接存 PDF / 存长图

**v1.3.0**
- 新增两套简历版式（现代简约 / 深色商务），PDF 与图片导出均套用
- 下载改为 **PDF（推荐）** + .doc，移除 .md
- 「附加简历文件」改为 **发送简历图片** 到 HR 对话
- 设置页新增版式选择与云端更新地址

**v1.2.0**
- 支持 PDF / Word 简历解析；话术多模板；优化后自动生成岗位话术
- 新增云端自动更新机制

**v1.1.0**
- 新增「选岗」：按简历匹配度筛选职位列表

**v1.0.0**
- 初版：JD 捕获 + 简历 AI 优化 + 快捷投递

---

## ⚖️ 免责声明

本工具仅用于个人求职信息整理与文本润色，请遵守 Boss 直聘用户协议，合理控制使用频率，对投递内容的真实性负责。AI 生成内容请务必人工核验。

MIT License
